import DialogBase from "./dialogBase.ts"

export default class EdmondDialog extends DialogBase {
    constructor() {
        super(['I\'m Edmond',
        'Life here is pretty hard',
        'we are surrounded by woods filled with monsters'])
    }
}