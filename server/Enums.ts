export enum Command {
    Login = 1,
    Move,
    Ping,
    Pong,
    Error,
}

export enum Direction {
    Up = 1,
    Down,
    Left,
    Right,
}

export enum Rooms {
    Initial = 0,
    Woods,
}